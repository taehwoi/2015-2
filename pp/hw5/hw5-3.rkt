#lang racket

(require "hw5-1.rkt")
(require "hw5-2.rkt")

(provide is-empty? fst rest length nth-elmt map reduce)

(define (is-empty? l)
  'TODO)

(define (fst l)
  'TODO)

(define (rest l)
  'TODO)

(define (length l)
  'TODO)

(define (nth-elmt l i)
  'TODO)

(define (map f l)
  'TODO)

(define (reduce l f s)
  'TODO)
